# CMM GUI Macros: Ordered Lists

# Args: ItemKey (string)
# Returns: string
macro = {
    description = "Owner setting key for one list item key"
    definition = "CMMListItemOwnerSettingKey(ItemKey)"
    replace_with = "CMMMetadataFlag('cmm_list_item_owner_setting_', ItemKey)"
}

# Args: ItemKey (string)
# Returns: none
macro = {
    description = "Execute the owner setting callback for one list item"
    definition = "CMMExecuteListItemSettingChanged(ItemKey)"
    replace_with = "CMMExecuteSettingChanged(CMMListItemOwnerSettingKey(ItemKey))"
}

# Args: SettingKey (string)
# Returns: boolean
macro = {
    description = "Cached interactive state for one list setting, written by cmm_can_edit_list_*; per-setting keys stop one list reading another's"
    definition = "CMMListCanEdit(SettingKey)"
    replace_with = "GetVariableSystem.HasValue(Concatenate(SettingKey, '_cel'), 'yes')"
}

# Args: ItemKey (string)
# Returns: boolean
macro = {
    description = "Cached interactive state for the setting owning one list item"
    definition = "CMMListItemCanEdit(ItemKey)"
    replace_with = "CMMListCanEdit(CMMListItemOwnerSettingKey(ItemKey))"
}

# Args: SettingKey (string)
# Returns: int32
macro = {
    description = "List field count for one setting"
    definition = "CMMListFieldCount(SettingKey)"
    replace_with = "CMMMetadataInt('cmm_list_field_count_', SettingKey)"
}

# Args: SettingKey (string)
# Returns: boolean
macro = {
    description = "True when one list setting shows row move controls"
    definition = "CMMListSettingIsOrdered(SettingKey)"
    replace_with = "CMMValueEqualsOne(CMMMapValue('cmm_list_is_ordered', SettingKey))"
}

# Args: ItemKey (string)
# Returns: boolean
macro = {
    description = "True when one list item belongs to a reorderable list setting"
    definition = "CMMListItemSettingIsOrdered(ItemKey)"
    replace_with = "CMMListSettingIsOrdered(CMMListItemOwnerSettingKey(ItemKey))"
}

# Args: SettingKey (string), SlotKey (string), Suffix (string)
# Returns: variable
macro = {
    description = "List field metadata variable accessor"
    definition = "CMMListFieldMetadataVar(SettingKey,SlotKey,Suffix)"
    replace_with = "CMMVar(Concatenate(Concatenate('cmm_list_field_', SlotKey), Concatenate(Suffix, SettingKey)))"
}

# Args: SettingKey (string), SlotKey (string), Suffix (string)
# Returns: string
macro = {
    description = "List field metadata flag-name accessor"
    definition = "CMMListFieldMetadataFlag(SettingKey,SlotKey,Suffix)"
    replace_with = "CMMListFieldMetadataVar(SettingKey, SlotKey, Suffix).GetFlagName"
}

# Args: SettingKey (string), SlotKey (string), Suffix (string)
# Returns: value
macro = {
    description = "List field metadata value accessor"
    definition = "CMMListFieldMetadataValue(SettingKey,SlotKey,Suffix)"
    replace_with = "CMMListFieldMetadataVar(SettingKey, SlotKey, Suffix).GetValue"
}

# Args: SettingKey (string), SlotKey (string), Suffix (string)
# Returns: int32
macro = {
    description = "List field metadata int accessor"
    definition = "CMMListFieldMetadataInt(SettingKey,SlotKey,Suffix)"
    replace_with = "FixedPointToInt(CMMListFieldMetadataValue(SettingKey, SlotKey, Suffix))"
}

# Args: ItemKey (string), SlotKey (string)
# Returns: variable
macro = {
    description = "List item field variable accessor"
    definition = "CMMListItemFieldVar(ItemKey,SlotKey)"
    replace_with = "CMMHomeScope.GetVariableFromVariableMap('cmm', MakeScopeFlag(Concatenate(ItemKey, Concatenate('_f', SlotKey))))"
}

# Args: ItemKey (string), SlotKey (string)
# Returns: value
macro = {
    description = "List item field value accessor"
    definition = "CMMListItemFieldValue(ItemKey,SlotKey)"
    replace_with = "Select_CFixedPoint(GetVariableSystem.HasValue(Concatenate(ItemKey, Concatenate('_isg_', SlotKey)), 'yes'), GetVariableFromGlobalVariableMap('cmm', MakeScopeFlag(Concatenate(ItemKey, Concatenate('_f', SlotKey)))).GetValue, CMMListItemFieldVar(ItemKey, SlotKey).GetValue)"
}

# Args: ItemKey (string), SlotKey (string)
# Returns: int32
macro = {
    description = "List item field int accessor"
    definition = "CMMListItemFieldInt(ItemKey,SlotKey)"
    replace_with = "FixedPointToInt(CMMListItemFieldValue(ItemKey, SlotKey))"
}

# Args: ItemKey (string), SlotKey (string)
# Returns: int32
macro = {
    description = "0-based widget index for one 1-based list dropdown field value"
    definition = "CMMListItemFieldWidgetIndex(ItemKey,SlotKey)"
    replace_with = "CMMOrdinalToWidgetIndex(CMMListItemFieldInt(ItemKey, SlotKey))"
}

# Args: SettingKey (string), SlotKey (string)
# Returns: boolean
macro = {
    description = "True when one list field slot is bool"
    definition = "CMMListFieldIsBool(SettingKey,SlotKey)"
    replace_with = "EqualTo_CFixedPoint(CMMListFieldMetadataValue(SettingKey, SlotKey, '_type_'), '(CFixedPoint)1')"
}

# Args: SettingKey (string), SlotKey (string)
# Returns: boolean
macro = {
    description = "True when one list field slot is data (display-only)"
    definition = "CMMListFieldIsData(SettingKey,SlotKey)"
    replace_with = "EqualTo_CFixedPoint(CMMListFieldMetadataValue(SettingKey, SlotKey, '_type_'), '(CFixedPoint)5')"
}

# Args: SettingKey (string), SlotKey (string)
# Returns: boolean
macro = {
    description = "True when one list field slot is a button"
    definition = "CMMListFieldIsButton(SettingKey,SlotKey)"
    replace_with = "EqualTo_CFixedPoint(CMMListFieldMetadataValue(SettingKey, SlotKey, '_type_'), '(CFixedPoint)6')"
}

# Args: SettingKey (string), SlotKey (string)
# Returns: boolean
macro = {
    description = "True when one list field slot is display-only text"
    definition = "CMMListFieldIsText(SettingKey,SlotKey)"
    replace_with = "EqualTo_CFixedPoint(CMMListFieldMetadataValue(SettingKey, SlotKey, '_type_'), '(CFixedPoint)7')"
}

# Args: SettingKey (string), SlotKey (string)
# Returns: boolean
macro = {
    description = "True when one list field has sign-conditional prefix/postfix format (has_format >= 2)"
    definition = "CMMListFieldHasConditionalFormat(SettingKey,SlotKey)"
    replace_with = "GreaterThan_CFixedPoint(CMMListFieldMetadataValue(SettingKey, SlotKey, '_has_format_'), '(CFixedPoint)1')"
}

# Args: SettingKey (string), ItemKey (string), SlotKey (string)
# Returns: string
macro = {
    description = "Sign-conditional prefix: selects _prefix_high (>0), _prefix_low (<0), or _prefix (==0) without existence checks"
    definition = "CMMListFieldConditionalPrefix(SettingKey,ItemKey,SlotKey)"
    replace_with = "Select_CString(GreaterThan_CFixedPoint(CMMListItemFieldValue(ItemKey, SlotKey), '(CFixedPoint)0'), CMMListFieldPrefixHigh(SettingKey, SlotKey), Select_CString(LessThan_CFixedPoint(CMMListItemFieldValue(ItemKey, SlotKey), '(CFixedPoint)0'), CMMListFieldPrefixLow(SettingKey, SlotKey), AddTextIf(CMMListFieldHasPrefix(SettingKey, SlotKey), CMMListFieldPrefix(SettingKey, SlotKey))))"
}

# Args: SettingKey (string), ItemKey (string), SlotKey (string)
# Returns: string
macro = {
    description = "Sign-conditional postfix: selects _postfix_high (>0), _postfix_low (<0), or _postfix (==0) without existence checks"
    definition = "CMMListFieldConditionalPostfix(SettingKey,ItemKey,SlotKey)"
    replace_with = "Select_CString(GreaterThan_CFixedPoint(CMMListItemFieldValue(ItemKey, SlotKey), '(CFixedPoint)0'), CMMListFieldPostfixHigh(SettingKey, SlotKey), Select_CString(LessThan_CFixedPoint(CMMListItemFieldValue(ItemKey, SlotKey), '(CFixedPoint)0'), CMMListFieldPostfixLow(SettingKey, SlotKey), AddTextIf(CMMListFieldHasPostfix(SettingKey, SlotKey), CMMListFieldPostfix(SettingKey, SlotKey))))"
}

# Args: SettingKey (string), ItemKey (string), SlotKey (string)
# Returns: string
macro = {
    description = "Resolved affix prefix: conditional when has_format >= 2, else plain prefix"
    definition = "CMMListFieldResolvedPrefix(SettingKey,ItemKey,SlotKey)"
    replace_with = "Select_CString(CMMListFieldHasConditionalFormat(SettingKey, SlotKey), CMMListFieldConditionalPrefix(SettingKey, ItemKey, SlotKey), AddTextIf(CMMListFieldHasPrefix(SettingKey, SlotKey), CMMListFieldPrefix(SettingKey, SlotKey)))"
}

# Args: SettingKey (string), ItemKey (string), SlotKey (string)
# Returns: string
macro = {
    description = "Resolved affix postfix: conditional when has_format >= 2, else plain postfix"
    definition = "CMMListFieldResolvedPostfix(SettingKey,ItemKey,SlotKey)"
    replace_with = "Select_CString(CMMListFieldHasConditionalFormat(SettingKey, SlotKey), CMMListFieldConditionalPostfix(SettingKey, ItemKey, SlotKey), AddTextIf(CMMListFieldHasPostfix(SettingKey, SlotKey), CMMListFieldPostfix(SettingKey, SlotKey)))"
}

# Args: SettingKey (string), SlotKey (string)
# Returns: int32
macro = {
    description = "Pixel width for one list field cell (pre-computed at registration)"
    definition = "CMMListFieldWidth(SettingKey,SlotKey)"
    replace_with = "CMMListFieldMetadataInt(SettingKey, SlotKey, '_width_')"
}

# Args: SettingKey (string), SlotKey (string)
# Returns: int32
macro = {
    description = "Max text width for one list field header (pre-computed at registration)"
    definition = "CMMListFieldHeaderMaxWidth(SettingKey,SlotKey)"
    replace_with = "CMMListFieldMetadataInt(SettingKey, SlotKey, '_header_max_width_')"
}

# Args: SettingKey (string), SlotKey (string)
# Returns: string
macro = {
    description = "Base localization key for one list field (e.g. mod__setting__field)"
    definition = "CMMListFieldLocKey(SettingKey,SlotKey)"
    replace_with = "CMMListFieldMetadataFlag(SettingKey, SlotKey, '_key_')"
}

# Args: SettingKey (string), SlotKey (string)
# Returns: boolean
macro = {
    description = "True when a _desc localization key exists for one list field"
    definition = "CMMListFieldHasDesc(SettingKey,SlotKey)"
    replace_with = "Not(EqualTo_string(Localize(Concatenate(CMMListFieldLocKey(SettingKey, SlotKey), '_desc')), Concatenate(CMMListFieldLocKey(SettingKey, SlotKey), '_desc')))"
}

# Args: SettingKey (string), SlotKey (string)
# Returns: string
macro = {
    description = "Resolved _desc localization text for one list field"
    definition = "CMMListFieldDescText(SettingKey,SlotKey)"
    replace_with = "Localize(Concatenate(CMMListFieldLocKey(SettingKey, SlotKey), '_desc'))"
}

# Args: SettingKey (string), SlotKey (string)
# Returns: string
macro = {
    description = "Resolved _text caption for one list button field"
    definition = "CMMListFieldButtonText(SettingKey,SlotKey)"
    replace_with = "Localize(Concatenate(CMMListFieldLocKey(SettingKey, SlotKey), '_text'))"
}

# Args: SettingKey (string), SlotKey (string)
# Returns: boolean
macro = {
    description = "True when a _postfix localization key exists for one list field"
    definition = "CMMListFieldHasPostfix(SettingKey,SlotKey)"
    replace_with = "Not(EqualTo_string(Localize(Concatenate(CMMListFieldLocKey(SettingKey, SlotKey), '_postfix')), Concatenate(CMMListFieldLocKey(SettingKey, SlotKey), '_postfix')))"
}

# Args: SettingKey (string), SlotKey (string)
# Returns: string
macro = {
    description = "Resolved _postfix localization text for one list field"
    definition = "CMMListFieldPostfix(SettingKey,SlotKey)"
    replace_with = "Localize(Concatenate(CMMListFieldLocKey(SettingKey, SlotKey), '_postfix'))"
}

# Args: SettingKey (string), SlotKey (string)
# Returns: boolean
macro = {
    description = "True when a _prefix localization key exists for one list field"
    definition = "CMMListFieldHasPrefix(SettingKey,SlotKey)"
    replace_with = "Not(EqualTo_string(Localize(Concatenate(CMMListFieldLocKey(SettingKey, SlotKey), '_prefix')), Concatenate(CMMListFieldLocKey(SettingKey, SlotKey), '_prefix')))"
}

# Args: SettingKey (string), SlotKey (string)
# Returns: string
macro = {
    description = "Resolved _prefix localization text for one list field"
    definition = "CMMListFieldPrefix(SettingKey,SlotKey)"
    replace_with = "Localize(Concatenate(CMMListFieldLocKey(SettingKey, SlotKey), '_prefix'))"
}

# Args: SettingKey (string), SlotKey (string)
# Returns: string
macro = {
    description = "Resolved _postfix_low localization text for one list field"
    definition = "CMMListFieldPostfixLow(SettingKey,SlotKey)"
    replace_with = "Localize(Concatenate(CMMListFieldLocKey(SettingKey, SlotKey), '_postfix_low'))"
}

# Args: SettingKey (string), SlotKey (string)
# Returns: string
macro = {
    description = "Resolved _prefix_low localization text for one list field"
    definition = "CMMListFieldPrefixLow(SettingKey,SlotKey)"
    replace_with = "Localize(Concatenate(CMMListFieldLocKey(SettingKey, SlotKey), '_prefix_low'))"
}

# Args: SettingKey (string), SlotKey (string)
# Returns: string
macro = {
    description = "Resolved _postfix_high localization text for one list field"
    definition = "CMMListFieldPostfixHigh(SettingKey,SlotKey)"
    replace_with = "Localize(Concatenate(CMMListFieldLocKey(SettingKey, SlotKey), '_postfix_high'))"
}

# Args: SettingKey (string), SlotKey (string)
# Returns: string
macro = {
    description = "Resolved _prefix_high localization text for one list field"
    definition = "CMMListFieldPrefixHigh(SettingKey,SlotKey)"
    replace_with = "Localize(Concatenate(CMMListFieldLocKey(SettingKey, SlotKey), '_prefix_high'))"
}

# Args: ItemKey (string), SlotKey (string)
# Returns: boolean
macro = {
    description = "True when one list item field is disabled for its slot"
    definition = "CMMListItemFieldIsDisabled(ItemKey,SlotKey)"
    replace_with = "CMMValueEqualsOne(CMMMapValue('cmm_list_field_disabled', Concatenate(ItemKey, Concatenate('_f', SlotKey))))"
}

# Args: ItemKey (string), SlotKey (string)
# Returns: boolean
macro = {
    description = "True when one list item field is enabled for its slot"
    definition = "CMMListItemFieldIsEnabled(ItemKey,SlotKey)"
    replace_with = "Not(CMMListItemFieldIsDisabled(ItemKey, SlotKey))"
}

# Args: ItemKey (string), SlotKey (string)
# Returns: variable
macro = {
    description = "Button field tab target map entry for one list item slot"
    definition = "CMMListItemButtonTabVar(ItemKey,SlotKey)"
    replace_with = "CMMHomeScope.GetVariableFromVariableMap('cmm_list_button_tab', MakeScopeFlag(Concatenate(ItemKey, Concatenate('_f', SlotKey))))"
}

# Args: ItemKey (string), SlotKey (string)
# Returns: boolean
macro = {
    description = "True when one list item button field opens a tab"
    definition = "CMMListItemButtonOpensTab(ItemKey,SlotKey)"
    replace_with = "CMMListItemButtonTabVar(ItemKey, SlotKey).IsSet"
}

# Args: ItemKey (string), SlotKey (string)
# Returns: string
macro = {
    description = "Tab key one list item button field opens"
    definition = "CMMListItemButtonTab(ItemKey,SlotKey)"
    replace_with = "CMMListItemButtonTabVar(ItemKey, SlotKey).GetFlagName"
}

# Args: ItemKey (string), SlotKey (string)
# Returns: string
macro = {
    description = "Settings-bearing tab key one list item button field opens, resolving a tab with sub-tabs to the sub-tab it opens on"
    definition = "CMMListItemButtonTabLeaf(ItemKey,SlotKey)"
    replace_with = "CMMTabLeaf(CMMListItemButtonTab(ItemKey, SlotKey))"
}

# Args: ItemKey (string), SlotKey (string)
# Returns: string
macro = {
    description = "Variable system key recording a list item button's tab as its parent's open sub-tab, falling back to the selected tab key itself when that tab has no parent to remember it"
    definition = "CMMListItemButtonSubTabKey(ItemKey,SlotKey)"
    replace_with = "Select_CString(CMMTabHasParent(CMMListItemButtonTabLeaf(ItemKey, SlotKey)), Concatenate(CMMTabParent(CMMListItemButtonTabLeaf(ItemKey, SlotKey)), '_lsub'), 'cmm_selected_tab')"
}

# Args: ItemKey (string)
# Returns: boolean
macro = {
    description = "True when one list item row is hidden"
    definition = "CMMListItemIsHidden(ItemKey)"
    replace_with = "CMMValueEqualsOne(CMMMapValue('cmm_list_item_hidden', ItemKey))"
}

# Args: ItemKey (string)
# Returns: boolean
macro = {
    description = "True when one list item row is visible"
    definition = "CMMListItemIsVisible(ItemKey)"
    replace_with = "Not(CMMListItemIsHidden(ItemKey))"
}

# Args: SettingKey (string), SlotKey (string)
# Returns: CFixedPoint
macro = {
    description = "Normalized slider maximum for one list field slot (max minus min)"
    definition = "CMMListFieldSliderNormalizedMax(SettingKey,SlotKey)"
    replace_with = "Subtract_CFixedPoint(CMMListFieldMetadataValue(SettingKey, SlotKey, '_max_'), CMMListFieldMetadataValue(SettingKey, SlotKey, '_min_'))"
}

# Args: ItemKey (string), SlotKey (string)
# Returns: CFixedPoint
macro = {
    description = "Normalized slider value for one list item field (value minus min)"
    definition = "CMMListFieldSliderNormalizedValue(ItemKey,SlotKey)"
    replace_with = "Subtract_CFixedPoint(CMMListItemFieldValue(ItemKey, SlotKey), CMMListFieldMetadataValue(CMMListItemOwnerSettingKey(ItemKey), SlotKey, '_min_'))"
}

# Args: ItemKey (string), SlotKey (string)
# Returns: int32
macro = {
    description = "Slider thumb visual index on 0-100 scale for one list item field"
    definition = "CMMListSliderThumbIndex(ItemKey,SlotKey)"
    replace_with = "FixedPointToInt(Add_CFixedPoint(Multiply_CFixedPoint(Divide_CFixedPoint(CMMListFieldSliderNormalizedValue(ItemKey, SlotKey), CMMListFieldSliderNormalizedMax(CMMListItemOwnerSettingKey(ItemKey), SlotKey)), '(CFixedPoint)100'), '(CFixedPoint)0.500'))"
}

# Cached per-cell resolved affix written by cmm_cache_field_cell; per-item keys stop list settings sharing it.
# Args: ItemKey (string), SlotStr (string)
# Returns: string
macro = {
    description = "Cached: resolved affix prefix for one list item field cell"
    definition = "CMMCachedResolvedPrefix(ItemKey,SlotStr)"
    replace_with = "GetVariableSystem.Get(Concatenate(ItemKey, Concatenate('_rpfx_', SlotStr)))"
}

# Args: ItemKey (string), SlotStr (string)
# Returns: string
macro = {
    description = "Cached: resolved affix postfix for one list item field cell"
    definition = "CMMCachedResolvedPostfix(ItemKey,SlotStr)"
    replace_with = "GetVariableSystem.Get(Concatenate(ItemKey, Concatenate('_rpofx_', SlotStr)))"
}
