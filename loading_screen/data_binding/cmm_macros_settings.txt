# CMM GUI Macros: Settings

# Args: SettingKey (string)
# Returns: boolean
macro = {
    description = "True when a setting key has been registered (type > 0)"
    definition = "CMMSettingIsRegistered(SettingKey)"
    replace_with = "GreaterThan_CFixedPoint(CMMMapValue('cmm_type', SettingKey), '(CFixedPoint)0')"
}

# Args: SettingKey (string)
# Returns: boolean
macro = {
    description = "True when a setting key is registered as global"
    definition = "CMMSettingIsGlobal(SettingKey)"
    replace_with = "CMMValueEqualsOne(CMMMapValue('cmm_is_global', SettingKey))"
}

# Args: SettingKey (string)
# Returns: boolean
macro = {
    description = "True when a setting key is registered as bool type"
    definition = "CMMSettingIsBool(SettingKey)"
    replace_with = "EqualTo_CFixedPoint(CMMMapValue('cmm_type', SettingKey), '(CFixedPoint)1')"
}

# Args: SettingKey (string)
# Returns: boolean
macro = {
    description = "True when a setting key is registered as dropdown type"
    definition = "CMMSettingIsDropdown(SettingKey)"
    replace_with = "EqualTo_CFixedPoint(CMMMapValue('cmm_type', SettingKey), '(CFixedPoint)2')"
}

# Args: SettingKey (string)
# Returns: boolean
macro = {
    description = "True when a setting key is registered as numeric type"
    definition = "CMMSettingIsNumeric(SettingKey)"
    replace_with = "EqualTo_CFixedPoint(CMMMapValue('cmm_type', SettingKey), '(CFixedPoint)3')"
}

# Args: SettingKey (string)
# Returns: boolean
macro = {
    description = "True when a setting key is registered as slider type"
    definition = "CMMSettingIsSlider(SettingKey)"
    replace_with = "EqualTo_CFixedPoint(CMMMapValue('cmm_type', SettingKey), '(CFixedPoint)4')"
}

# Args: SettingKey (string)
# Returns: boolean
macro = {
    description = "True when a setting key is registered as text type"
    definition = "CMMSettingIsText(SettingKey)"
    replace_with = "EqualTo_CFixedPoint(CMMMapValue('cmm_type', SettingKey), '(CFixedPoint)5')"
}

# Args: SettingKey (string)
# Returns: boolean
macro = {
    description = "True when a setting key is registered as button type"
    definition = "CMMSettingIsButton(SettingKey)"
    replace_with = "EqualTo_CFixedPoint(CMMMapValue('cmm_type', SettingKey), '(CFixedPoint)6')"
}

# Args: SettingKey (string)
# Returns: boolean
macro = {
    description = "True when a setting key is registered as list type"
    definition = "CMMSettingIsList(SettingKey)"
    replace_with = "EqualTo_CFixedPoint(CMMMapValue('cmm_type', SettingKey), '(CFixedPoint)7')"
}

# Args: SettingKey (string)
# Returns: boolean
macro = {
    description = "True when a setting key requires the Unrestricted Tools setting to be enabled"
    definition = "CMMSettingRequiresUnrestrictedToolsEnabled(SettingKey)"
    replace_with = "CMMValueEqualsOne(CMMMapValue('cmm_requires_unrestricted_tools_enabled', SettingKey))"
}

# A console-command setting is exempt from needing a player country, because the console
# delivers it rather than a scripted GUI Execute.
# Args: SettingKey (string)
# Returns: boolean
macro = {
    description = "True when local player can edit this setting"
    definition = "CMMCanEditSetting(SettingKey)"
    replace_with = "And(Or(CMFHasPlayerCountry, CMMSettingHasConsoleCommand(SettingKey)), And(Or(Not(CMMSettingIsGlobal(SettingKey)), Or(Not(GameIsMultiplayer), IsHost)), Or(Not(CMMSettingRequiresUnrestrictedToolsEnabled(SettingKey)), Or(EqualTo_string(SettingKey, 'cmf_core__enable_unrestricted_tools'), And(CMMSettingIsRegistered('cmf_core__enable_unrestricted_tools'), CMMValueEqualsOne(CMMSettingValue('cmf_core__enable_unrestricted_tools')))))))"
}

# Args: SettingKey (string)
# Returns: value
macro = {
    description = "Read current setting value from country or global scope"
    definition = "CMMSettingValue(SettingKey)"
    replace_with = "Select_CFixedPoint(CMMSettingIsGlobal(SettingKey), GetVariableFromGlobalVariableMap('cmm', MakeScopeFlag(SettingKey)).GetValue, CMMHomeScope.GetVariableFromVariableMap('cmm', MakeScopeFlag(SettingKey)).GetValue)"
}

# Args: SettingKey (string)
# Returns: boolean
macro = {
    description = "True when a setting has a registered scripted GUI"
    definition = "CMMHasScriptedGui(SettingKey)"
    replace_with = "CMMValueEqualsOne(CMMMapValue('cmm_has_sgui', SettingKey))"
}

# Args: SettingKey (string)
# Returns: none
macro = {
    description = "Execute setting on-changed scripted GUI callback"
    definition = "CMMExecuteSettingChanged(SettingKey)"
    replace_with = "CMMExecuteGui(Concatenate(SettingKey, '_on_changed'))"
}

# Args: SettingKey (string)
# Returns: boolean
macro = {
    description = "Checkbox checked state for bool settings (1 = on)"
    definition = "CMMIsSettingEnabled(SettingKey)"
    replace_with = "CMMValueEqualsOne(CMMSettingValue(SettingKey))"
}

# Args: SettingKey (string)
# Returns: int32
macro = {
    description = "Dropdown option count for one setting"
    definition = "CMMDropdownOptionCount(SettingKey)"
    replace_with = "CMMMapInt('cmm_dropdown_count', SettingKey)"
}

# Args: SettingKey (string)
# Returns: int32
macro = {
    description = "Stored 1-based dropdown selected ordinal for one setting"
    definition = "CMMDropdownStoredIndex(SettingKey)"
    replace_with = "FixedPointToInt(CMMSettingValue(SettingKey))"
}

# Args: SettingKey (string)
# Returns: int32
macro = {
    description = "0-based dropdown widget selected index for one setting"
    definition = "CMMDropdownSelectedIndex(SettingKey)"
    replace_with = "CMMOrdinalToWidgetIndex(CMMDropdownStoredIndex(SettingKey))"
}

# Args: SettingKey (string)
# Returns: boolean
macro = {
    description = "True when a dropdown setting is flagged to render with the multiselector layout"
    definition = "CMMDropdownIsMultiselector(SettingKey)"
    replace_with = "CMMValueEqualsOne(CMMMapValue('cmm_dropdown_multiselector', SettingKey))"
}

# Returns: boolean
macro = {
    description = "True when text settings may be edited in the current session"
    definition = "CMMCanEditTextSetting"
    replace_with = "And(Not(GameIsMultiplayer), CMFHasPlayerCountry)"
}

# Args: SettingKey (string)
# Returns: int32
macro = {
    description = "Text setting editbox character limit"
    definition = "CMMTextSettingCharacterLimit(SettingKey)"
    replace_with = "CMMMapInt('cmm_text_char_limit', SettingKey)"
}

# Args: SettingKey (string)
# Returns: boolean
macro = {
    description = "True when submitted text should be wrapped in double quotes"
    definition = "CMMTextSettingQuoteText(SettingKey)"
    replace_with = "CMMValueEqualsOne(CMMMapValue('cmm_text_quote', SettingKey))"
}

# Args: SettingKey (string)
# Returns: value
macro = {
    description = "Scrollbar-normalized max (max - min, always >= 0)"
    definition = "CMMSliderNormalizedMax(SettingKey)"
    replace_with = "Subtract_CFixedPoint(CMMMapValue('cmm_max', SettingKey), CMMMapValue('cmm_min', SettingKey))"
}

# Args: SettingKey (string)
# Returns: value
macro = {
    description = "Scrollbar-normalized value (value - min, always >= 0)"
    definition = "CMMSliderNormalizedValue(SettingKey)"
    replace_with = "Subtract_CFixedPoint(CMMSettingValue(SettingKey), CMMMapValue('cmm_min', SettingKey))"
}

# Args: SettingKey (string)
# Returns: int32
macro = {
    description = "Slider thumb visual index on 0-100 scale for one setting"
    definition = "CMMSliderThumbIndex(SettingKey)"
    replace_with = "FixedPointToInt(Add_CFixedPoint(Multiply_CFixedPoint(Divide_CFixedPoint(CMMSliderNormalizedValue(SettingKey), CMMSliderNormalizedMax(SettingKey)), '(CFixedPoint)100'), '(CFixedPoint)0.500'))"
}

# Args: ThumbIndex (int32), Offset (int32 literal)
# Returns: boolean
macro = {
    description = "True when hovered slider index equals thumb index plus offset"
    definition = "CMMSliderHoverMatch(ThumbIndex,Offset)"
    replace_with = "GetVariableSystem.HasValue('cmm_slider_hover_idx', ToString_int32(Add_int32(ThumbIndex, Offset)))"
}

# Args: ThumbIndex (int32), ScopeKey (string)
# Returns: boolean
macro = {
    description = "True when the hovered overlay index is within 5 of the thumb and the scope key matches"
    definition = "CMMSliderThumbHover(ThumbIndex,ScopeKey)"
    replace_with = "And(GetVariableSystem.HasValue('cmm_slider_hover_key', ScopeKey), Or(CMMSliderHoverMatch(ThumbIndex, '(int32)-5'), Or(CMMSliderHoverMatch(ThumbIndex, '(int32)-4'), Or(CMMSliderHoverMatch(ThumbIndex, '(int32)-3'), Or(CMMSliderHoverMatch(ThumbIndex, '(int32)-2'), Or(CMMSliderHoverMatch(ThumbIndex, '(int32)-1'), Or(CMMSliderHoverMatch(ThumbIndex, '(int32)0'), Or(CMMSliderHoverMatch(ThumbIndex, '(int32)1'), Or(CMMSliderHoverMatch(ThumbIndex, '(int32)2'), Or(CMMSliderHoverMatch(ThumbIndex, '(int32)3'), Or(CMMSliderHoverMatch(ThumbIndex, '(int32)4'), CMMSliderHoverMatch(ThumbIndex, '(int32)5'))))))))))))"
}

# Args: SettingKey (string)
# Returns: string
macro = {
    description = "Variable-system cache key for one text setting input field"
    definition = "CMMTextSettingInputCacheKey(SettingKey)"
    replace_with = "Concatenate('cmm_text_input_', SettingKey)"
}

# Args: SettingKey (string)
# Returns: string
macro = {
    description = "Scripted effect callback key for one text setting"
    definition = "CMMTextSettingCallbackKey(SettingKey)"
    replace_with = "Concatenate(SettingKey, '_on_changed')"
}

# Args: SettingKey (string)
# Returns: value
macro = {
    description = "Setting value by key, taking the key as an argument so it does not need the Scope datacontext localization has no access to"
    definition = "CMMV(SettingKey)"
    replace_with = "Select_CFixedPoint(EqualTo_CFixedPoint(GetCountry(GetVariableSystem.Get('cmm_home_tag')).MakeScope.GetVariableFromVariableMap('cmm_is_global', MakeScopeFlag(SettingKey)).GetValue, '(CFixedPoint)1'), GetVariableFromGlobalVariableMap('cmm', MakeScopeFlag(SettingKey)).GetValue, GetCountry(GetVariableSystem.Get('cmm_home_tag')).MakeScope.GetVariableFromVariableMap('cmm', MakeScopeFlag(SettingKey)).GetValue)"
}

# Args: SettingKey (string)
# Returns: boolean
macro = {
    description = "True when a button setting opens the log view"
    definition = "CMMSettingTogglesLogView(SettingKey)"
    replace_with = "EqualTo_string(SettingKey, 'cmf_core__open_log')"
}

# Args: SettingKey (string)
# Returns: boolean
macro = {
    description = "True when a setting has an associated console command to fire on toggle"
    definition = "CMMSettingHasConsoleCommand(SettingKey)"
    replace_with = "EqualTo_string(SettingKey, 'cmf_core__enable_debug_mode')"
}

# Args: SettingKey (string)
# Returns: string
macro = {
    description = "Console command string for a setting with an associated console command"
    definition = "CMMSettingConsoleCommand(SettingKey)"
    replace_with = "Select_CString(EqualTo_string(SettingKey, 'cmf_core__enable_debug_mode'), 'debug_mode', '')"
}

# Args: SettingKey (string)
# Returns: boolean
macro = {
    description = "True when a _format localization key exists for a setting"
    definition = "CMMHasFormat(SettingKey)"
    replace_with = "Not(EqualTo_string(Localize(Concatenate(SettingKey, '_format')), Concatenate(SettingKey, '_format')))"
}

# Args: SettingKey (string)
# Returns: string
macro = {
    description = "Formatted setting value if _format localization exists, raw value otherwise"
    definition = "CMMDisplayValue(SettingKey)"
    replace_with = "Localize(Concatenate(SettingKey, '_format'))"
}

# Cached setting-row metadata, written once per row by the cmm_cache_setting_row state and read
# cheaply per frame. Valid only inside a setting-row context (the row and its child controls).

# Args: SettingKey (string), TypeStr (string)
# Returns: boolean
macro = {
    description = "Cached: true when a setting row's type equals TypeStr (1 bool, 2 dropdown, 3 numeric, 4 slider, 5 text, 6 button, 7 list)"
    definition = "CMMCachedSettingTypeIs(SettingKey,TypeStr)"
    replace_with = "GetVariableSystem.HasValue(Concatenate(SettingKey, '_srt'), TypeStr)"
}

# Args: SettingKey (string)
# Returns: string
macro = {
    description = "Cached: localized name for one setting row"
    definition = "CMMCachedSettingName(SettingKey)"
    replace_with = "GetVariableSystem.Get(Concatenate(SettingKey, '_srn'))"
}

# Args: SettingKey (string)
# Returns: string
macro = {
    description = "Cached: localized description for one setting row"
    definition = "CMMCachedSettingDesc(SettingKey)"
    replace_with = "GetVariableSystem.Get(Concatenate(SettingKey, '_srd'))"
}

# Args: SettingKey (string)
# Returns: boolean
macro = {
    description = "Cached: true when one setting row has a description"
    definition = "CMMCachedSettingHasDesc(SettingKey)"
    replace_with = "GetVariableSystem.HasValue(Concatenate(SettingKey, '_srhd'), 'yes')"
}

# Args: SettingKey (string)
# Returns: string
macro = {
    description = "Cached: localized button caption for one setting row"
    definition = "CMMCachedSettingButtonText(SettingKey)"
    replace_with = "GetVariableSystem.Get(Concatenate(SettingKey, '_srbtn'))"
}

# Args: SettingKey (string)
# Returns: boolean
macro = {
    description = "Cached: true when a dropdown setting renders with the multiselector layout"
    definition = "CMMCachedDropdownIsMultiselector(SettingKey)"
    replace_with = "GetVariableSystem.HasValue(Concatenate(SettingKey, '_srmulti'), 'yes')"
}

# Args: SettingKey (string)
# Returns: boolean
macro = {
    description = "Cached visibility rule for one setting row; the scripted-GUI is_shown stays live"
    definition = "CMMCachedSettingRowVisible(SettingKey)"
    replace_with = "And(CMMMatchesSelectedModAndTab(GetVariableSystem.Get(Concatenate(SettingKey, '_srom')), GetVariableSystem.Get(Concatenate(SettingKey, '_srot'))), Or(Not(GetVariableSystem.HasValue(Concatenate(SettingKey, '_srsgui'), 'yes')), CMMGuiIsShown(Concatenate(SettingKey, '_on_changed'))))"
}

# Args: SettingKey (string)
# Returns: boolean
macro = {
    description = "Cached edit-permission rule for one setting row; the unrestricted-tools-enabled state stays live"
    definition = "CMMCachedCanEditSetting(SettingKey)"
    replace_with = "And(Or(CMFHasPlayerCountry, CMMSettingHasConsoleCommand(SettingKey)), And(Or(Not(GetVariableSystem.HasValue(Concatenate(SettingKey, '_srisg'), 'yes')), Or(Not(GameIsMultiplayer), IsHost)), Or(Not(GetVariableSystem.HasValue(Concatenate(SettingKey, '_srrut'), 'yes')), Or(EqualTo_string(SettingKey, 'cmf_core__enable_unrestricted_tools'), And(CMMSettingIsRegistered('cmf_core__enable_unrestricted_tools'), CMMValueEqualsOne(CMMSettingValue('cmf_core__enable_unrestricted_tools')))))))"
}

# Args: SettingKey (string)
# Returns: boolean
macro = {
    description = "Cached: true when a setting has a callback scripted GUI (non-list settings register one; list settings always have one)"
    definition = "CMMCachedSettingHasCallbackGui(SettingKey)"
    replace_with = "Or(GetVariableSystem.HasValue(Concatenate(SettingKey, '_srsgui'), 'yes'), CMMCachedSettingTypeIs(SettingKey, '7'))"
}

# Args: SettingKey (string)
# Returns: boolean
macro = {
    description = "Cached is_valid rule for one setting control; the scripted-GUI is_valid stays live"
    definition = "CMMCachedSettingIsValid(SettingKey)"
    replace_with = "Or(Not(CMMCachedSettingHasCallbackGui(SettingKey)), CMMGuiIsValid(Concatenate(SettingKey, '_on_changed')))"
}

# Args: SettingKey (string)
# Returns: boolean
macro = {
    description = "Cached: true when a setting control is interactive (edit permission and the scripted-GUI is_valid)"
    definition = "CMMCachedSettingEnabled(SettingKey)"
    replace_with = "And(CMMCachedCanEditSetting(SettingKey), CMMCachedSettingIsValid(SettingKey))"
}

# Args: SettingKey (string)
# Returns: boolean
macro = {
    description = "Cached: true when a _format localization key exists for a setting"
    definition = "CMMCachedHasFormat(SettingKey)"
    replace_with = "GetVariableSystem.HasValue(Concatenate(SettingKey, '_srhf'), 'yes')"
}
