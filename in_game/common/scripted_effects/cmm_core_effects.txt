# Pre-initialize all CMM variable maps so quoted scope links never access a nonexistent map.
# Idempotent: re-running leaves the maps in the same seeded state.
# Scope: country
_cmm_init_variable_maps = {
	add_to_variable_map = { name = cmm key = flag:cmm_init value = 0 }
	add_to_variable_map = { name = cmm_type key = flag:cmm_init value = 0 }
	add_to_variable_map = { name = cmm_is_global key = flag:cmm_init value = 0 }
	add_to_variable_map = { name = cmm_default key = flag:cmm_init value = 0 }
	add_to_variable_map = { name = cmm_min key = flag:cmm_init value = 0 }
	add_to_variable_map = { name = cmm_max key = flag:cmm_init value = 0 }
	add_to_variable_map = { name = cmm_step key = flag:cmm_init value = 0 }
	add_to_variable_map = { name = cmm_dropdown_count key = flag:cmm_init value = 0 }
	add_to_variable_map = { name = cmm_dropdown_last_index key = flag:cmm_init value = 0 }
	add_to_variable_map = { name = cmm_slider_actual_step_count key = flag:cmm_init value = 0 }
	add_to_variable_map = { name = cmm_slider_actual_last_index key = flag:cmm_init value = 0 }
	add_to_variable_map = { name = cmm_text_char_limit key = flag:cmm_init value = 0 }
	add_to_variable_map = { name = cmm_text_quote key = flag:cmm_init value = 0 }
	add_to_variable_map = { name = cmm_list_count key = flag:cmm_init value = 0 }
	add_to_variable_map = { name = cmm_list_last_index key = flag:cmm_init value = 0 }
	add_to_variable_map = { name = cmm_list_is_ordered key = flag:cmm_init value = 0 }
	add_to_variable_map = { name = cmm_has_sgui key = flag:cmm_init value = 0 }
	add_to_variable_map = { name = cmm_no_reset key = flag:cmm_init value = 0 }
	add_to_variable_map = { name = cmm_requires_unrestricted_tools_enabled key = flag:cmm_init value = 0 }
	add_to_variable_map = { name = cmm_list_field_disabled key = flag:cmm_init value = 0 }
	add_to_variable_map = { name = cmm_list_item_hidden key = flag:cmm_init value = 0 }
	add_to_variable_map = { name = cmm_list_button_tab key = flag:cmm_init value = flag:cmm_init }
	add_to_variable_map = { name = cmm_setting_owner_mod key = flag:cmm_init value = flag:cmm_init }
	add_to_variable_map = { name = cmm_list_field_default key = flag:cmm_init value = 0 }
	add_to_variable_map = { name = cmm_list_field_default_pending key = flag:cmm_init value = 0 }
	add_to_variable_map = { name = cmm_list_field_format_pending key = flag:cmm_init value = 0 }
	add_to_variable_map = { name = cmm_list_field_loc_name_pending key = flag:cmm_init value = flag:cmm_init }
	add_to_variable_map = { name = cmm_list_field_loc_key_pending key = flag:cmm_init value = flag:cmm_init }
	add_to_variable_map = { name = cmm_list_field_owner key = flag:cmm_init value = flag:cmm_init }
	add_to_variable_map = { name = cmm_setting_owner_tab key = flag:cmm_init value = flag:cmm_init }
	add_to_variable_map = { name = cmm_setting_owner_group key = flag:cmm_init value = flag:cmm_init }
	add_to_variable_map = { name = cmm_group_owner_tab key = flag:cmm_init value = flag:cmm_init }
	add_to_variable_map = { name = cmm_tab_parent key = flag:cmm_init value = flag:cmm_init }
	add_to_variable_map = { name = cmm_tab_default_child key = flag:cmm_init value = flag:cmm_init }
	add_to_variable_map = { name = cmm_setting_shown key = flag:cmm_init value = 0 }
	add_to_variable_map = { name = cmm_group_nonempty key = flag:cmm_init value = 0 }
	add_to_variable_map = { name = cmm_tab_nonempty key = flag:cmm_init value = 0 }
	add_to_variable_map = { name = cmm_mod_nonempty key = flag:cmm_init value = 0 }
	add_to_variable_map = { name = cmm_group_visible_count key = flag:cmm_init value = 0 }
	add_to_variable_map = { name = cmm_group_collapsed key = flag:cmm_init value = 0 }
	add_to_global_variable_map = { name = cmm key = flag:cmm_init value = 0 }

	# Benign warning suppression
	if = {
		limit = {
			always = no
			has_variable_map = cmm
			has_variable_map = cmm_type
			has_variable_map = cmm_is_global
			has_variable_map = cmm_default
			has_variable_map = cmm_min
			has_variable_map = cmm_max
			has_variable_map = cmm_step
			has_variable_map = cmm_dropdown_count
			has_variable_map = cmm_dropdown_last_index
			has_variable_map = cmm_slider_actual_step_count
			has_variable_map = cmm_slider_actual_last_index
			has_variable_map = cmm_text_char_limit
			has_variable_map = cmm_text_quote
			has_variable_map = cmm_list_count
			has_variable_map = cmm_list_last_index
			has_variable_map = cmm_list_is_ordered
			has_variable_map = cmm_has_sgui
			has_variable_map = cmm_no_reset
			has_variable_map = cmm_requires_unrestricted_tools_enabled
			has_variable_map = cmm_list_field_disabled
			has_variable_map = cmm_list_item_hidden
			has_variable_map = cmm_list_button_tab
			has_variable_map = cmm_setting_owner_mod
			has_variable_map = cmm_list_field_default
			has_variable_map = cmm_list_field_default_pending
			has_variable_map = cmm_list_field_format_pending
			has_variable_map = cmm_list_field_loc_name_pending
			has_variable_map = cmm_list_field_loc_key_pending
			has_variable_map = cmm_list_field_owner
			has_variable_map = cmm_setting_owner_tab
			has_variable_map = cmm_setting_owner_group
			has_variable_map = cmm_group_owner_tab
			has_variable_map = cmm_tab_parent
			has_variable_map = cmm_tab_default_child
			has_variable_map = cmm_setting_shown
			has_variable_map = cmm_group_nonempty
			has_variable_map = cmm_tab_nonempty
			has_variable_map = cmm_mod_nonempty
			has_variable_map = cmm_group_visible_count
			has_variable_map = cmm_group_collapsed
			has_global_variable_map = cmm
		}
		cmf_suppress = { v = cmm_init }
	}
}

# Clear the per-registration-pass scratch state at the start of every registration
# cycle, before cmf_on_mod_registration fires. The seeded-keys list lets a per-item
# default set after registration drive the initial value; the loc/format stores let
# those config effects apply before or after registration. Clearing each pass keeps
# loc/format reverting to field-id defaults when a mod stops asserting them. Item
# hidden, field disabled, button tab target, and sub-tab parent state clears each
# pass too; mods re-assert it during registration.
# Scope: country
_cmm_clear_registration_pass_state = {
	clear_variable_list = cmm_list_field_seeded_this_pass
	clear_variable_map = cmm_list_field_loc_name_pending
	clear_variable_map = cmm_list_field_loc_key_pending
	clear_variable_map = cmm_list_field_format_pending
	clear_variable_map = cmm_list_item_hidden
	clear_variable_map = cmm_list_button_tab
	clear_variable_map = cmm_list_field_disabled
	clear_variable_map = cmm_tab_parent
	clear_variable_map = cmm_tab_default_child
	add_to_variable_map = { name = cmm_list_field_loc_name_pending key = flag:cmm_init value = flag:cmm_init }
	add_to_variable_map = { name = cmm_list_field_loc_key_pending key = flag:cmm_init value = flag:cmm_init }
	add_to_variable_map = { name = cmm_list_field_format_pending key = flag:cmm_init value = 0 }
	add_to_variable_map = { name = cmm_list_item_hidden key = flag:cmm_init value = 0 }
	add_to_variable_map = { name = cmm_list_button_tab key = flag:cmm_init value = flag:cmm_init }
	add_to_variable_map = { name = cmm_list_field_disabled key = flag:cmm_init value = 0 }
	add_to_variable_map = { name = cmm_tab_parent key = flag:cmm_init value = flag:cmm_init }
	add_to_variable_map = { name = cmm_tab_default_child key = flag:cmm_init value = flag:cmm_init }
}

# Register a mod (idempotent by mod_id).
# Scope: country
# Args:
# - mod_id (key): Mod identifier; derives `<mod_id>_name` and `<mod_id>_desc`.
_cmm_register_mod = {
	_cmm_init_variable_maps = yes
	set_variable = { name = $mod_id$_name value = flag:$mod_id$_name }
	set_variable = { name = $mod_id$_desc value = flag:$mod_id$_desc }

	# Ensure mod is in the id list (rebuilt each registration cycle).
	if = {
		limit = { NOT = { is_target_in_variable_list = { name = cmm_registered_mod_ids target = flag:$mod_id$ } } }
		add_to_variable_list = {
			name = cmm_registered_mod_ids
			target = flag:$mod_id$
		}
		cmf_register_mod = { mod_id = $mod_id$ }
		# Reset default tab so the first registered tab becomes default this cycle.
		remove_variable = cmm_mod_default_tab_$mod_id$
	}

	# Benign warning suppression
	if = {
		limit = { always = no }
		cmf_suppress = { v = $mod_id$ }
		cmf_suppress = { v = $mod_id$_name }
		cmf_suppress = { v = $mod_id$_desc }
		cmf_suppress = { v = cmm_registered_mod_ids }
	}
}

# Register a tab for a mod and implicitly register the owner mod (idempotent by mod_id + tab_id).
# Scope: country
# Args:
# - mod_id (key): Owner mod id.
# - tab_id (key): Tab id unique within mod.
_cmm_register_tab = {
	_cmm_register_mod = {
		mod_id = $mod_id$
	}

	# Registration path for this tab key (list rebuilt each cycle).
	if = {
		limit = { NOT = { is_target_in_variable_list = { name = cmm_registered_tab_keys target = flag:$mod_id$__$tab_id$ } } }
		set_variable = { name = cmm_tab_owner_mod_id_$mod_id$__$tab_id$ value = flag:$mod_id$ }
		set_variable = { name = $mod_id$__$tab_id$_name value = flag:$mod_id$__$tab_id$_name }

		# Global tab key list; used by right-panel tab datamodel iteration.
		add_to_variable_list = {
			name = cmm_registered_tab_keys
			target = flag:$mod_id$__$tab_id$
		}

		# First registered tab becomes default selected tab for this mod.
		if = {
			limit = { NOT = { has_variable = cmm_mod_default_tab_$mod_id$ } }
			set_variable = { name = cmm_mod_default_tab_$mod_id$ value = flag:$mod_id$__$tab_id$ }
		}
	}

	# Benign warning suppression
	if = {
		limit = { always = no }
		cmf_suppress = { v = $mod_id$__$tab_id$ }
		cmf_suppress = { v = $mod_id$__$tab_id$_name }
		cmf_suppress = { v = cmm_registered_tab_keys }
		cmf_suppress = { v = cmm_mod_default_tab_$mod_id$ }
		cmf_suppress = { v = cmm_tab_owner_mod_id_$mod_id$__$tab_id$ }
	}
}

# Register a tab as a sub-tab of another tab, implicitly registering both tabs and the owner mod.
# A tab with sub-tabs shows only its sub-tabs; settings registered on it directly do not render.
# Scope: country
# Args:
# - mod_id (key): Owner mod id.
# - tab_id (key): Sub-tab id unique within mod.
# - parent_tab_id (key): Parent tab id within mod.
cmm_register_subtab = {
	_cmm_register_tab = {
		mod_id = $mod_id$
		tab_id = $parent_tab_id$
	}

	_cmm_register_tab = {
		mod_id = $mod_id$
		tab_id = $tab_id$
	}

	add_to_variable_map = { name = cmm_tab_parent key = flag:$mod_id$__$tab_id$ value = flag:$mod_id$__$parent_tab_id$ }

	# Where the parent's tab button lands when clicked.
	if = {
		limit = { NOT = { is_key_in_variable_map = { name = cmm_tab_default_child target = flag:$mod_id$__$parent_tab_id$ } } }
		add_to_variable_map = { name = cmm_tab_default_child key = flag:$mod_id$__$parent_tab_id$ value = flag:$mod_id$__$tab_id$ }
	}
}

# Register a group for a mod tab and implicitly register the owner tab/mod (idempotent by mod_id + tab_id + group_id).
# Scope: country
# Args:
# - mod_id (key): Owner mod id.
# - tab_id (key): Owner tab id within mod.
# - group_id (key): Group id unique within tab.
_cmm_register_group = {
	_cmm_register_tab = {
		mod_id = $mod_id$
		tab_id = $tab_id$
	}

	# Registration path for this group key (list rebuilt each cycle).
	if = {
		limit = { NOT = { is_target_in_variable_list = { name = cmm_registered_group_keys target = flag:$mod_id$__$tab_id$__$group_id$ } } }
		set_variable = { name = cmm_group_owner_mod_id_$mod_id$__$tab_id$__$group_id$ value = flag:$mod_id$ }
		set_variable = { name = cmm_group_owner_tab_key_$mod_id$__$tab_id$__$group_id$ value = flag:$mod_id$__$tab_id$ }
		add_to_variable_map = { name = cmm_group_owner_tab key = flag:$mod_id$__$tab_id$__$group_id$ value = flag:$mod_id$__$tab_id$ }
		set_variable = { name = $mod_id$__$tab_id$__$group_id$_name value = flag:$mod_id$__$tab_id$__$group_id$_name }
		set_variable = { name = $mod_id$__$tab_id$__$group_id$_desc value = flag:$mod_id$__$tab_id$__$group_id$_desc }

		# Clear per-group setting list so stale settings from old saves are removed.
		clear_variable_list = cmm_group_setting_keys_$mod_id$__$tab_id$__$group_id$

		# Global group key list; used by settings-panel group datamodel iteration.
		add_to_variable_list = {
			name = cmm_registered_group_keys
			target = flag:$mod_id$__$tab_id$__$group_id$
		}
	}

	# Benign warning suppression
	if = {
		limit = { always = no }
		cmf_suppress = { v = $mod_id$__$tab_id$__$group_id$ }
		cmf_suppress = { v = $mod_id$__$tab_id$__$group_id$_name }
		cmf_suppress = { v = $mod_id$__$tab_id$__$group_id$_desc }
		cmf_suppress = { v = $mod_id$__$tab_id$ }
		cmf_suppress = { v = cmm_registered_group_keys }
		cmf_suppress = { v = cmm_group_owner_mod_id_$mod_id$__$tab_id$__$group_id$ }
		cmf_suppress = { v = cmm_group_owner_tab_key_$mod_id$__$tab_id$__$group_id$ }
		cmf_suppress = { v = cmm_group_owner_tab }
		cmf_suppress = { v = cmm_group_setting_keys_$mod_id$__$tab_id$__$group_id$ }
	}
}

# Register shared metadata for a setting key and implicitly register the owner group/tab/mod (idempotent by mod_id + setting_id).
# Scope: country
# Args:
# - mod_id (key): Owner mod id.
# - setting_id (key): Setting id unique within mod.
# - tab_id (key): Owner tab id within mod.
# - group_id (key): Owner group id within tab.
_cmm_register_setting_metadata = {
	_cmm_register_group = {
		mod_id = $mod_id$
		tab_id = $tab_id$
		group_id = $group_id$
	}

	# Registration path for this setting key (list rebuilt each cycle).
	if = {
		limit = { NOT = { is_target_in_variable_list = { name = cmm_registered_setting_keys target = flag:$mod_id$__$setting_id$ } } }
		set_variable = { name = cmm_setting_owner_mod_id_$mod_id$__$setting_id$ value = flag:$mod_id$ }
		set_variable = { name = cmm_setting_owner_tab_key_$mod_id$__$setting_id$ value = flag:$mod_id$__$tab_id$ }
		set_variable = { name = cmm_setting_owner_group_key_$mod_id$__$setting_id$ value = flag:$mod_id$__$tab_id$__$group_id$ }
		add_to_variable_map = { name = cmm_setting_owner_mod key = flag:$mod_id$__$setting_id$ value = flag:$mod_id$ }
		add_to_variable_map = { name = cmm_setting_owner_tab key = flag:$mod_id$__$setting_id$ value = flag:$mod_id$__$tab_id$ }
		add_to_variable_map = { name = cmm_setting_owner_group key = flag:$mod_id$__$setting_id$ value = flag:$mod_id$__$tab_id$__$group_id$ }
		set_variable = { name = $mod_id$__$setting_id$_name value = flag:$mod_id$__$setting_id$_name }
		set_variable = { name = $mod_id$__$setting_id$_desc value = flag:$mod_id$__$setting_id$_desc }

		# Global setting key list; used by right-panel datamodel iteration.
		add_to_variable_list = {
			name = cmm_registered_setting_keys
			target = flag:$mod_id$__$setting_id$
		}

		# Per-group setting key list; used by group-scoped setting rendering.
		add_to_variable_list = {
			name = cmm_group_setting_keys_$mod_id$__$tab_id$__$group_id$
			target = flag:$mod_id$__$setting_id$
		}
	}

	# Benign warning suppression
	if = {
		limit = { always = no }
		cmf_suppress = { v = $mod_id$__$setting_id$ }
		cmf_suppress = { v = $mod_id$__$tab_id$ }
		cmf_suppress = { v = $mod_id$__$tab_id$__$group_id$ }
		cmf_suppress = { v = $mod_id$__$setting_id$_name }
		cmf_suppress = { v = $mod_id$__$setting_id$_desc }
		cmf_suppress = { v = $mod_id$__$tab_id$__$group_id$_name }
		cmf_suppress = { v = cmm_registered_setting_keys }
		cmf_suppress = { v = cmm_conditional_setting_keys }
		cmf_suppress = { v = cmm_setting_owner_mod_id_$mod_id$__$setting_id$ }
		cmf_suppress = { v = cmm_setting_owner_tab_key_$mod_id$__$setting_id$ }
		cmf_suppress = { v = cmm_setting_owner_group_key_$mod_id$__$setting_id$ }
		cmf_suppress = { v = cmm_group_setting_keys_$mod_id$__$tab_id$__$group_id$ }
	}
}

# Mark a setting as having a scripted GUI and track it for the empty-container rollup.
# Scope: country
# Args:
# - mod_id (key): Owner mod id.
# - setting_id (key): Setting id.
_cmm_mark_setting_has_sgui = {
	add_to_variable_map = { name = cmm_has_sgui key = flag:$mod_id$__$setting_id$ value = 1 }
	if = {
		limit = { NOT = { is_target_in_variable_list = { name = cmm_conditional_setting_keys target = flag:$mod_id$__$setting_id$ } } }
		add_to_variable_list = { name = cmm_conditional_setting_keys target = flag:$mod_id$__$setting_id$ }
	}
}

# Register a scripted GUI for a setting (enables is_shown/is_valid checks).
# The scripted GUI must be named `<mod_id>__<setting_id>_on_changed`.
# Call after the setting registration effect.
# Scope: country
# Args:
# - mod_id (key): Owner mod id.
# - setting_id (key): Setting id.
cmm_add_scripted_gui = {
	_cmm_mark_setting_has_sgui = { mod_id = $mod_id$ setting_id = $setting_id$ }
}

# Mark a setting as non-resettable (excluded from Reset to Defaults).
# Call after the setting registration effect.
# Scope: country
# Args:
# - mod_id (key): Owner mod id.
# - setting_id (key): Setting id.
cmm_set_no_reset = {
	add_to_variable_map = { name = cmm_no_reset key = flag:$mod_id$__$setting_id$ value = 1 }
}

# Mark a setting as requiring the Unrestricted Tools setting to be enabled (editable only when that setting is on).
# Use for settings outside normal play (e.g. debug/cheat-style tools). Call after the setting registration effect.
# Scope: country
# Args:
# - mod_id (key): Owner mod id.
# - setting_id (key): Setting id.
cmm_set_requires_unrestricted_tools_enabled = {
	add_to_variable_map = { name = cmm_requires_unrestricted_tools_enabled key = flag:$mod_id$__$setting_id$ value = 1 }
}

